# dataset path
root: /mnt/data_1/Dataset/Temp/bdd100k_images_annotations

# output path
target: /mnt/data_1/Dataset/Temp/bdd100k_3_classes_20211126

# input labels style: cityscapes, coco2017, huawei_segment, bdd100k, apolloscape_lane_segment, hy_val, cvat_coco2017, yunce_segment
srstyle: bdd100k

# output labels style: cityscapes, coco2017, cityscapes_val, cvat_image_1_1
tarstyle: cityscapes

# images or labels need to rename prefix
prefix: "bdd100k"

# name delimiter(cityscapes: "@")
delimiter: "@"

# dataset segment class path
classes: /home/leidi/hy_program/2D_Dataset_clean/Segmentation_dataset_clean_up/data/segment_classes_bdd100k_11_classes.names

# dataset class modify file path
modifyclassfile: /home/leidi/hy_program/2D_Dataset_clean/Segmentation_dataset_clean_up/data/segment_classes_bdd100k_11_classes_to_3_classes_road_lane.txt
# modifyclassfile: ""

# (train, val, test, redund)
proportion: 0.92,0.04,0.04,0
# proportion: 0.8,0.1,0.1,0

# check target annotations
check: 100

# transparent bounding box mask
mask: True

# total class true box pixel limits, use to limit class pixel and distance
classpixeldistance: ""

# anchor box cluster count
anchorboxcluster: 9

# progress debug
debug: False
